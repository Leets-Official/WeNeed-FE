import { dndLinkIcon, dndSoundIcon } from 'ui/IconsPath';

export const linkType = {
  customStyle: '',
  iconInfo: dndLinkIcon,
} as const;

export const soundType = {
  customStyle: 'px-[22px] gap-x-[16px]',
  iconInfo: dndSoundIcon,
} as const;
